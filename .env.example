# Copy this file to .env and fill in what you need, then:
#   docker compose up -d

# Editor PIN for /edit. Leave blank to use ./data/edit-pin (sqndash --set-pin) instead.
EDIT_PIN=

# TimeTree credentials are entered on the /edit page (Calendar source → TimeTree),
# not via environment variables.
